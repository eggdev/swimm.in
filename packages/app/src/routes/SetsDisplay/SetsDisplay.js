import React from 'react';
import {View, Text} from 'react-native-ui-lib';

/**
 *  SetsDisplay Component
 */
const SetsDisplay = (props) => {
  return (
    <View>
      <Text>SetsDisplay</Text>
    </View>
  );
};

SetsDisplay.propTypes = {};

export default SetsDisplay;
